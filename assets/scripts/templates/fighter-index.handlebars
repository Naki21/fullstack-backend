<div class="col-lg-12">
  <h1 class="page-header">Your Barracks</h1>
  <p> Here you can set your current fighter or delete them</p>
</div>
{{#each fighters as |fighter|}}
  <div class="col-lg-3 col-md-4 col-xs-6 thumb" id="fighter-{{fighter.id}}">
    <a class="thumbnail">
      <img class="img-responsive" src="{{fighter.url}}" alt="{{fighter.url}}">
      <button type="button" id="create-battle-button" data-fighter-id='{{fighter.id}}' class="btn btn-success btn-xs">Set</button>
      <button type="button" data-fighter-id='{{fighter.id}}' class="btn btn-danger btn-xs">Delete</button>

    </a>
  </div>
{{/each}}
<div class="col-xs-12">
  <hr>
<button type="button" id="get-fighter-button" class="btn btn-primary btn">Get New Fighter</button>
</div>
